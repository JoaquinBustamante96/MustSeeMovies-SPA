import { OnloadDirective } from './onload.directive';

describe('OnloadDirective', () => {
  it('should create an instance', () => {
    const directive = new OnloadDirective();
    expect(directive).toBeTruthy();
  });
});
