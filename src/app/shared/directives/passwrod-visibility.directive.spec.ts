import { PasswrodVisibilityDirective } from './passwrod-visibility.directive';

describe('PasswrodVisibilityDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswrodVisibilityDirective();
    expect(directive).toBeTruthy();
  });
});
