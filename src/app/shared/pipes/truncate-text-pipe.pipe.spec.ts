import { TruncateTextPipePipe } from './truncate-text-pipe.pipe';

describe('TruncateTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TruncateTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
